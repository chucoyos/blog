<div class='flex flex-col'>
<h1>Article</h1>
<div class='p-4 bg-slate-600 rounded-sm mb-4'>
  <h3><%= @article.title %></h3>
  <p><%= @article.body %>
</div>
<div>
  <%= link_to 'Delete article', @article, data: { turbo_method: :delete, turbo_confirm: 'Are you sure?'}, class: 'border border-slate-400 px-2 mt-1 rounded-sm' %>
  <%= link_to 'home', root_path, class: 'border border-slate-400 px-2 mt-1 rounded-sm' %>
</div>

