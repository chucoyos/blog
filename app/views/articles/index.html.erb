<% @articles.each do |article| %>
  <h1><%= link_to article.title, article%></h1>
  <div>
    <h4><%= article.body%> -
    <%= link_to "Eliminar", article, method: :delete %></h4>
    <p>Visitas: <%= article.visits_count%></p>
    </div>
<% end %>