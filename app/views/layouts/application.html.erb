<!DOCTYPE html>
<html>
  <head>
    <title>Blog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class='bg-slate-900 text-slate-100 relative text-sm'>
    <%= render 'layouts/nav' %>
    <main class="container mt-16 mx-auto px-5 flex w-full">
      <% if notice %>
        <div class='absolute top-14 right-8 py-2 px-6 rounded <%= "bg-green-950" if notice.present? %>'>
          <div class='flex gap-2 justify-between'>
            <p class='opacity-100 text-green-100'><%= notice %></p>
             <%= link_to 'x' %>
          </div>
        </div>
      <% end %>
      <%= yield %>
    </main>
  </body>
</html>
