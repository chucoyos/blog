<!DOCTYPE html>
<html>
  <head>
    <title>Blog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class='bg-slate-900 text-slate-100 relative text-sm'>
    <%= render 'layouts/nav' %>
    <main class="container mt-28 mx-auto px-5 flex w-full">
      <div class='absolute top-8 right-8 py-2 px-6 rounded <%= "bg-green-900" if notice.present? %>'>
        <%= notice %>
      </div>
      <%= yield %>
    </main>
  </body>
</html>
