<li id='<%= comment.id %>', class='flex gap-2 items-center bg-slate-500 rounded-xl py-1 px-2'>
  <div class='flex gap-2'>
    <p class='font-semibold'>👦️ <%= comment.commenter %></p> 
    <p> <%= comment.body %> </p>
  </div>
  <%= link_to '🗑️', [comment.article, comment], 
      data: {turbo_method: :delete, turbo_confirm: 'Delete comment?'}, 
      class: 'bg-slate-700 rounded ml-auto'%>
</li>